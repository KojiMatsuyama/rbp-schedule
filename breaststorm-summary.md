# ブレストまとめ — RBPシステム理論モデルと実装の検証

---

## 1. 理論モデルの提示

### 1.1 システム原理

ユーザーが提唱するシステムの基本原理は、以下の3つの命題で表現される。

```
状態{トークン} → ナビゲーター{判断＋実働指示}

判断 = 認知 − 評価 − 決定（仕様決定）− 投射（テンプレートインスタンス化）

状態 = マーキング機能（状態を記録可能）
```

この原理を言葉で説明すると、以下のようになる。

**「世界の状態をトークンとして表現し、それが充足されるとナビゲーターが自動発火する。ナビゲーターは判断を行い、その判断結果を実働指示として出力する。」**

これは一言で言えば「刺激と反射」のネットワークである。ただし、反射が単なる即座の反応ではなく、**多段階の判断プロセス**を経由する点が重要。

#### 判断の4段階パイプライン

判断は4つの段階を経て行われる。それぞれの段階は独立した関数（ナビゲーターの一部）として実装され、前の段階の出力が入力として次の段階に渡される。

| 段階 | 名前 | 役割 | 出力 |
|------|------|------|------|
| 第1段階 | **認知** | 入力データの正当性を検証する | OK / NG |
| 第2段階 | **評価** | 要求の重大度・優先度を評価する | 評価スコア |
| 第3段階 | **決定（仕様決定）** | 代数計算で具体的な仕様パラメータを決定する | 仕様パラメータ |
| 第4段階 | **投射（テンプレートインスタンス化）** | テンプレートにパラメータを埋め込み、実行可能な手順を生成する | 作業手順ナビ |

各段階について、以下で詳しく説明する。

#### 認知（Cognition）

認知は、入力されたトークンの正当性をチェックするフィルタリング段階である。

具体的には、入力された病害虫の組み合わせが既知のものかどうかを検証する。未知の組み合わせ（UNDEFINED）が入力された場合、それは認知NGとなり、処理はそこで終了する。

認知がOKの場合のみ、次の評価段階に進む。

**認知の重要性**: 不正な入力に対する防御策。未知の入力がシステム内を流れることで、予期せぬ挙動やエラーを引き起こすのを防ぐ。

#### 評価（Evaluation）

評価は、要求の重大度や優先度を評価する段階である。

RBP（Relation-Based Programming）の行列演算を用いて、入力された状態トークンを代数計算し、要求の評価スコアを算出する。この計算は決定論的であり、同じ入力に対して常に同じ結果を返す。

評価の結果は、次の決定段階に入力される。

#### 決定／仕様決定（Decision / Specification Determination）

決定は、代数設計されたRBP行列演算によって、具体的な仕様パラメータを決定する段階である。

評価段階からの入力を受け取り、どのような仕様の作動が必要かを計算する。例えば、「農薬Aを50ml使用し、面積10aに対して散布する」といった具体的なパラメータを出力する。

この計算は完全な決定論的プロセスであり、ブラックボックスではない。なぜその仕様になったのかを追跡可能である。

#### 投射／テンプレートインスタンス化（Projection / Template Instantiation）

投射は、決定段階で出力された仕様パラメータを受け取り、具体的な手順＝ナビゲーションを生成する段階である。

**重要な点**: 投射は単一のモジュールではなく、**対象ごと・バージョンごとに個別に準備する複数のテンプレートモジュールからなる**。

**投射が所有する手順の数**: 旧設計では「投射は1つの手順のみを所有する」と定義していたが、これを**「投射は2つ以上の手順を所有可能」**に変更する。この変更により、並列性（分業）が実装される。

```
投射の構造（変更後）:

投射 {
    projection_id: string
    // 旧: procedure: Procedure          ← 1つだけ
    // 新: procedures: Procedure[]       ← 2つ以上可能
    procedures: [procedureA, procedureB, procedureC, ...]
    // 各procedureは並列に実行される
}
```

**並列性と分岐の責任分離**:

```
分岐（パッケージ切り替えなど）:
  仕様決定 → [選択ブリッジA] → 投射A（E2X用）
            [選択ブリッジB] → 投射B（Apex用）
            ← ブリッジ層で分岐。1つのブリッジが1つの投射を選ぶ

並列（分業）:
  仕様決定 → [選択ブリッジ] → 投射（手順A + 手順B + 手順C）
            ← 1つのブリッジが1つの投射を選び、その投射内で並列
```

- **ブリッジ**は相変わらず1つの仕様しか決定しない
- その仕様が「手順を複数持つ投射」を選べば、その投射内の手順が**すべて並列に実行される**
- 合流（AND-join）はPetri netの定義通り：次のtransitionが入力placeのトークンが揃うまで待つ
- 分岐（パッケージ切り替えなど）はブリッジ層で、並列（分業）は投射層で扱う

##### テンプレートモジュールの具体例

**メール送信エージェントの場合:**
メールを送信するには、以下のテンプレートモジュールが必要。
- **本文テンプレート**: メール本文の骨格。宛名、挨拶、本文、結びなどのプレースホルダを含む
- **宛先テンプレート**: 送信先アドレスのリスト。To, CC, BCCの区分けを含む
- **件名テンプレート**: 件名の生成規則。件名のフォーマットや条件分岐を含む

これらのテンプレートに、仕様決定で出力されたパラメータ（送信先アドレス、件名、本文内容など）を埋め込むことで、実際に送信可能なメールが完成する。

**EDIソフト操作エージェントの場合:**
同じEDIソフトでも、バージョンが異なればAPIが異なる。代表的な例として、E2XとApexがある。

- **EDI-E2X操作テンプレート群**: E2X固有のAPIエンドポイント、パラメータ形式、認証方式に対応したテンプレート
  - 取引データエクスポートテンプレート
  - 注文書インポートテンプレート
  - 納品書連携テンプレート
- **EDI-Apex操作テンプレート群**: Apex固有のAPIエンドポイント、パラメータ形式、認証方式に対応したテンプレート
  - 取引データエクスポートテンプレート
  - 注文書インポートテンプレート
  - 納品書連携テンプレート

E2XとApexは同じEDIソフトファミリーだが、APIの呼び出し方が全く異なる。したがって、**別々のテンプレートモジュールを事前に準備する必要がある**。

**散布ドローン制御テンプレート群:**
- 航路計画テンプレート: 飛行経路の生成規則
- 流量制御テンプレート: 農薬の噴射量と速度の制御規則
- 着陸判定テンプレート: 安全な着陸地点の選定規則

##### テンプレート選択ブリッジ

では、**どのテンプレートモジュールを使うか**をどう決めるのか。

答えは、**仕様決定フェーズと投射フェーズの間にブリッジを置く**ことである。このブリッジが、仕様決定の結果を見て「どのテンプレートを使うべきか」を判断する。

このブリッジの選択基準は、**境界言語（Domain Specific Language: DSL）**で明示的に定義される。

例えば、以下のような定義を考える。

```
境界言語でのテンプレート選択定義:

IF ソフトID == "E2X" AND バージョン >= "3.0" THEN
    テンプレートモジュール = "edi_e2x_v3_template_group"
ELSE IF ソフトID == "Apex" AND バージョン >= "5.0" THEN
    テンプレートモジュール = "edi_apex_v5_template_group"
ELSE IF エージェント種別 == "email" THEN
    テンプレートモジュール = "email_template_group"
END IF
```

このように、どのテンプレートを使うかは**暗黙ではなく明示的に定義**される。これが境界言語の役割である。

**分岐と並列の使い分け**:

```
分岐（パッケージ切り替え）:
  仕様決定 → [選択ブリッジ] → 複数の投射候補
  ← ブリッジ層で分岐。1つのブリッジが1つの投射を選ぶ

並列（分業）:
  仕様決定 → [選択ブリッジ] → 1つの投射
  ← その投射が複数の手順を所有 → 並列実行
```

- **分岐**はブリッジ層で扱う。パッケージ切り替えのように「 EITHER/OR 」の場合は、ブリッジが条件に応じて異なる投射を選ぶ
- **並列**は投射層で扱う。分業のように「 BOTH AND 」の場合は、1つの投射が複数の手順を並列に実行する

##### テンプレートインスタンス化のプロセス

テンプレートモジュールの選択が完了したら、実際にテンプレートにパラメータを埋め込む。

```
テンプレート: "農薬{drug_name}を{area}aに{dosage}ml散布"
↓ 仕様決定パラメータを埋め込む
drug_name = "マラソン乳剤"
dosage = 50
area = 10
↓ インスタンス化結果
"農薬マラソン乳剤を10aに50ml散布"
```

このインスタンス化結果が、最終的な「作業手順ナビ」として実働側に渡される。

#### Petri netの比喩

このシステムはPetri net（ペトリネット）の遷移（transition）の比喩で理解できる。

Petri netでは、トランジションにトークンが揃うと発火し、出力側のプレイスにトークンを配置する。RBPシステムでも同様に、状態{トークン}が充足するとナビゲーターが発火し、次の状態を生成する。

#### オブジェクト指向の比喩

オブジェクト指向で言えば、ナビゲーターは「メソッド」（クラスではなく）に近い。クラスは状態を持つが、ナビゲーター自体は状態を持たない（ステートレス）。より正確には**関数**である。

状態が充足（トークンが満たされる）と、次のナビゲーターが自動発火する。これは、関数が引数を受け取って戻り値を返すのと似ているが、関数チェーンが自動的に繋がる点が異なる。

---

### 1.2 記号界と物理界の二重構造

このシステムは**二つの世界**から構成される。

```
記号界（ナビゲーターネット）
    ↓ 指示
物理界（実働）
    ↓ 変化を検知
記号界（状態空間の更新）← 新しいトークン生成
    ↓
次のナビゲーター発火
```

この二重構造を理解することが、RBPシステムの核心である。

#### 記号界（Symbolic Realm）

記号界は、ナビゲーターが動く世界である。内容はトークン・行列・代数計算であり、存在形態は状態空間データネット上に存在する情報である。

記号界の中で何が起きるかと言えば、ナビゲーターの発火によって自発的に進む。つまり、何らかの状態（トークン）が変化すると、それに応じてナビゲーターが連鎖的に発火していく。

#### 物理界（Physical Realm）

物理界は、実働が起きる世界である。内容は人が動く・機械が動く・ソフトがAPIを叩くであり、存在形態は現実世界の出来事である。

物理界の変化は、そのままでは記号界には伝わらない。変化が検知され、記号化されて初めて、記号界に反映される。

#### 二つの世界の関係性

| 観点 | 記号界 → 物理界 | 物理界 → 記号界 |
|------|----------------|----------------|
| **方向** | 指示・命令 | 変化・報告 |
| **内容** | 作業手順ナビ | センサー値、完了通知、API応答 |
| **変換** | テンプレートインスタンス化 | 記号化（センサー値→トークン） |
| **遅延** | ほぼ即時 | 検知までに遅延があり得る |

#### フィードバックループ

物理界の変化が記号界の状態空間に反映される。これにより、次のナビゲーターが新しい状態で発火する。

このループが回ることで、システムは「判断 → 実働 → 変化検知 → 再判断」という継続的なサイクルを形成する。

```
┌─────────────────────────────────────────────────────┐
│                    フィードバックループ               │
│                                                     │
│  ① ナビゲーターが判断 → 作業手順ナビを生成           │
│  ② 実働側が手順に従って実行                          │
│  ③ 物理界の状態が変化する                            │
│  ④ 変化が検知され、記号化される（新しいトークン生成） │
│  ⑤ 新しいトークンが状態空間データネットに反映         │
│  ⑥ 新しい状態でナビゲーターが発火                    │
│  ① に戻る                                           │
└─────────────────────────────────────────────────────┘
```

---

### 1.3 三層アーキテクチャ：状態空間データネット × ナビゲーターネット × 物理界

記号界はさらに**二つの層**に分解できる。合わせて三層。

```
┌──────────────────────────────────────────────────────────────────┐
│                    状態空間データネット（記号界）                   │
│                                                                  │
│  ・トークン状態の記録・保持                                       │
│  ・ナビゲーターネットの状態（マーキング）を永続化                 │
│  ・ナビゲーターネットと常に連動                                    │
│  ・ナビゲーターは状態を持たない（ステートレス）                    │
│                                                                  │
└───────────────────────┬──────────────────────────────────────────┘
                        ↓ トークン入力
┌───────────────────────┴──────────────────────────────────────────┐
│                    ナビゲーターネット（記号界）                     │
│                                                                  │
│  ・純粋関数ネット                                                  │
│  ・モナドから解放された刺激・反射ネットワーク                       │
│  ・入力（トークン）→ 計算 → 出力（作業手順ナビ）                   │
│  ・内部状態を持たない                                              │
│                                                                  │
│  認知 → 評価 → 決定（仕様決定）→ 投射（テンプレートインスタンス化）                                       │
│                                                                  │
└───────────────────────┬──────────────────────────────────────────┘
                        ↓ 指示
┌───────────────────────┴──────────────────────────────────────────┐
│                    物理界（実働）                                  │
│                                                                  │
│  作業者 / 作業機械 / 作業ソフト                                   │
│       ↓                                                        │
│  物理界の状態が変化する                                           │
│       ↓                                                        │
│  変化が検知され、記号化される                                     │
│       ↓                                                        │
│  状態空間データネットに反映 ←─── フィードバックループ             │
└──────────────────────────────────────────────────────────────────┘
```

#### 各層の詳細

**第1層: 状態空間データネット**

状態空間データネットは、トークン状態の記録・保持を担当する層である。ナビゲーターネットの状態（マーキング）を永続化し、ナビゲーターネットと常に連動する。

重要な特性として、ナビゲーターは状態を持たない（ステートレス）ことが挙げられる。すべての状態管理はこの層に集中する。

**第2層: ナビゲーターネット**

ナビゲーターネットは、純粋関数ネットである。モナドから解放された刺激・反射ネットワークであり、入力（トークン）を受けて計算を行い、出力（作業手順ナビ）を返す。

内部状態を持たないため、同じ入力に対して常に同じ出力を返す。これは決定論的な振る舞いを保証する。

**第3層: 物理界**

物理界は、作業者・作業機械・作業ソフトが実際に動く世界である。ここでの状態変化が、フィードバックループを通じて記号界に反映される。

---

### 1.4 状態空間データネット × ナビゲーターネットの連動

```
状態空間データネット          ナビゲーターネット
┌─────────────────┐         ┌─────────────────┐
│ マーキング記録   │ ←─────→ │ トークン状態     │
│ （永続化）       │   連動   │ （揮発・計算用）  │
│                 │         │                 │
│ ・過去のマーキング│         │ ・純粋関数        │
│ ・時系列履歴     │         │ ・モナド不要      │
│ ・現在の状態     │         │ ・刺激→反射       │
└─────────────────┘         └─────────────────┘
```

- **状態空間データネット**がマーキングを記録する
- **ナビゲーターネット**はその記録を読み取って計算する
- ナビゲーターネット自身は状態を持たない（ステートレス）
- 結果として、ナビゲーターネットは**純粋関数ネット**になる

#### 連動の本質

状態空間データネットとナビゲーターネットは常に連動している。ナビゲーターネットの状態はトークン状態をマーキングするが、その記録も状態データネットに書かれる。

それによって、ナビゲーターネットは、完全にモナドから解放された純粋な関数ネットになり、刺激と反射によって動作するネットワークになる。

#### なぜステートレスなのか

ナビゲーターが内部状態を持たない理由はいくつかある。

1. **テスト容易性**: 内部状態がないため、同じ入力に対して常に同じ出力を返す。ユニットテストが容易。
2. **並列実行安全性**: 共有状態がないため、複数のナビゲーターが並列に実行されても競合が発生しない。
3. **デバッグ容易性**: 問題が発生した場合、入出力だけを追えばよい。中間状態を追う必要がない。
4. **スケーラビリティ**: 状態が外部にあるため、ナビゲーター自体は簡単にスケールアウトできる。

---

### 1.5 モナドからの解放

従来のシステムでは、状態管理がロジックに混入するため：

```
// モナドが必要な世界（状態がロジックの中にある）
navigate(input) {
    this.state.update(...)  // 副作用がある → 純粋関数ではない
    return result
}
```

状態空間データネットに分離することで：

```
// 純粋関数の世界（状態が外にある）
navigate(token, stateSnapshot) {
    // 入力tokenとstateSnapshotだけを使って計算
    // 副作用ゼロ、内部状態なし
    return navigation
}
```

ナビゲーターネットは**刺激と反射によって動作するネットワーク**になる。

#### モナドとは何か（補足）

モナドは、副作用付きの計算を純粋関数の枠組み内で扱うためのパターンである。例えば、IOモナドは入出力の副作用を扱い、Stateモナドは状態の変更を扱う。

RBPシステムでは、状態管理を状態空間データネットに完全に分離したため、ナビゲーターは副作用を持たない。その結果、モナドは不要になる。

これは大きな利点である。モナドを使わなくても、状態の推移を正しく追跡できる。

---

### 1.6 ナビゲーターと実働の分離

ナビゲーターは**判断のみ**を行い、**実働は行わない**。

```
┌─────────────────────────────────────────────────┐
│               ナビゲーター（判断パート）          │
│                                                 │
│  認知 → 評価 → 決定（仕様決定）→ 投射（テンプレートインスタンス化）                      │
│                     ↓                           │
│            作業手順ナビ（指示）                   │
└─────────────────────────────────────────────────┘
              ↓ 指示 ↓
┌──────────────┬──────────────┬──────────────┐
│              │              │              │
▼              ▼              ▼              ▼
作業者      作業機械      作業ソフト
（手順書）   （制御指令）   （操作API）
```

- **ナビゲーション** = 道を示すこと（航海・操舵・誘導・案内）
- ナビゲーターは**目的地に対して最適な経路を選び、進行方向を示す**
- 実働はナビゲーターの外で行われる

#### 分離の意義

ナビゲーターと実働を分離する意義は大きい。

1. **判断の透明性**: 判断ロジックと実行ロジックが分離されているため、どちらが問題の原因かを特定しやすい。
2. **柔軟性**: 実働側を変更しても、ナビゲーターに影響を与えない。逆に、ナビゲーターを変更しても、実働側に影響を与えない。
3. **再利用性**: 同じナビゲーターを異なる実働側と組み合わせて使える。

---

### 1.7 実働の3カテゴリ

| カテゴリ | 例 | ナビ出力形式 | テンプレートの特徴 |
|---------|-----|------------|------------------|
| **作業者** | 農業者、保守員 | 作業手順書（自然言語 + 数値 + 順序） | 人間が読みやすい自然言語。ステップ番号付き。注意点や警告を含む |
| **作業機械** | 散布ドローン、自動灌漑装置 | 制御指令（座標 + 流量 + 時間） | 機械が解釈できる構造化データ。JSONまたはXML形式。精度の高い座標値 |
| **作業ソフト** | 在庫管理システム、メール配信ツール | 操作API（パラメータ + 実行トリガー） | API呼び出しのパラメータ。エンドポイント、HTTPメソッド、ヘッダー、ボディ |

---

### 1.8 業務実態空間の構造：組織活動と分散状態管理

ナビゲーションによって実働者が操作される現実世界の活動領域は、単純な「機械が動く」の世界ではない。**この領域は「組織活動」として構造化されており、多様なアクターとリソースが絡み合った複雑系**である。

本書では、以降この領域を **業務実態空間（Business Reality Space）** と呼ぶ。業務実態空間とは、従事者（人・委託組織）、シーケンサー機械、AI機械、業務アプリケーション、センサー、実働環境信号といった実働アクターが実際に存在し、組織活動として連携しながら状態変化を起こしている現実の場のことである。1.2〜1.7で述べた「物理界」は記号界との対比における抽象概念（実働が起きる世界一般）であったのに対し、業務実態空間はその物理界の内部構造――誰が・何が・どのような関係で動いているか――を具体的に記述するための概念である。

**状態空間データネットは、この業務実態空間の範囲をカバーする存在である。** ただしこの「カバーする」は、業務実態空間に属する全アクターの状態を状態空間データネットが一元的に格納する、という意味ではない。1.8.3で述べる通り、業務実態空間の規模と多様性ゆえに単一データベースでの一元管理は技術的に不可能であり、各アクターは自らの状態を自らのデータベースで保持し続ける。状態空間データネットが「カバーする」とは、業務実態空間に含まれるアクターのうち信頼関係を結んだもの（1.8.4の「参加アクター」）を対象範囲として認識し、それらのトークンを参照可能にするという意味である。したがって、

```
状態空間データネットの管轄範囲 ⊆ 業務実態空間の外延
```

という包含関係が成り立つ。両者が完全に一致しないのは、業務実態空間の中に「孤立DBアクター」（状態空間データネットと未接続のアクター）が存在するためであり、1.8.5で述べる移行戦略は、この管轄範囲を業務実態空間の外延に近づけていくプロセスとして位置づけられる。

#### 1.8.1 業務実態空間のアクター分類

業務実態空間には、ナビゲーションによって操作される以下のアクターが存在する。

| アクター種別 | 名称 | 説明 | 状態管理の主体 |
|-------------|------|------|--------------|
| **従事者（人）** | 農業者、保守員、オペレーター | 人間の労働力。自然言語の手順書に従って作業する | 個人、または所属組織 |
| **従事者（委託組織）** | 外注業者、パートナー企業、サブコントラクター | 組織として契約に基づき作業を実施する | 委託元の組織DB |
| **シーケンサー機械** | PLC制御装置、産業用ロボット、自動搬送装置 | プログラムされた順序通りに動作する機械 | 機械内のコントローラ、または設備管理DB |
| **AI機械** | 自律ドローン、AI判別カメラ、予測保全システム | 機械学習モデルに基づく自律的判断を行う機械 | AIモデルのホストDB |
| **業務アプリケーション** | ERP、MES、在庫管理システム、メール配信ツール | ビジネスプロセスを支援するソフトウェア | アプリケーションのDB |
| **センサー** | 温湿度センサー、土壌水分センサー、画像センサー | 物理現象を電気信号に変換するデバイス | IoTプラットフォームDB |
| **実働環境信号** | 気象情報、地震計、洪水水位計、放射線計 | 自然界から来る環境データ | 気象庁、地方公共団体等の公的DB |

#### 1.8.2 組織活動としての業務実態空間

これらのアクターは、単独で存在するのではなく、**組織活動として連携して動く**。

```
業務実態空間：組織活動グラフ

[従事者（人）] ──雇用──→ [委託組織A]
                              │
                              ├──契約──→ [業務アプリ（ERP）]
                              │
                              ├──委託──→ [従事者（委託組織B）]
                              │                                    │
                              │                                    ├──契約──→ [業務アプリ（在庫管理）]
                              │                                    │
                              │                                    └──制御──→ [シーケンサー機械]
                              │
                              └──指示──→ [従事者（人・現場作業員）]
                                                   │
                                                   ├──操作──→ [AI機械（散布ドローン）]
                                                   │
                                                   └──監視──→ [センサー（土壌水分）]

[実働環境信号] ──提供──→ [センサー]
```

ナビゲーションは、この組織活動グラフの**任意のエッジ（関係性）に対して介入可能**である。

- 従事者に直接指示を出す
- 委託組織に発注を出す
- シーケンサー機械に制御指令を出す
- AI機械にタスクを割り当てる
- 業務アプリケーションにAPI呼び出しを出す
- センサーの設定パラメータを変更する
- 実働環境信号に基づく閾値を調整する

#### 1.8.3 分散状態管理の必然性

**核心的な洞察**: 業務実態空間の全状態を一つのデータベースで更新管理することは、現状の計算機資源や技術では不可能である。

なぜ不可能なのか。

**理由1: 業務実態空間の規模と多様性**

業務実態空間には膨大な数のアクターが存在し、それぞれが独自の状态を持っている。

```
例: 全国規模の農業管理システム

- 従事者（人）: 10,000人以上
- 従事者（委託組織）: 500組織以上
- シーケンサー機械: 5,000台以上
- AI機械: 200台以上
- 業務アプリケーション: 100系統以上
- センサー: 50,000個以上
- 実働環境信号: 1,000観測点以上

合計: 60,000以上のアクターが常に変化し続ける状態を管理する必要がある
```

一つのデータベースでこれら全てをリアルタイムに更新・整合性は、技術的にも経済的にも現実的ではない。

**理由2: 所有権とアクセス制御**

各アクターは異なる組織に属しており、異なる所有権を持つ。

```
[従事者Aの個人DB]          [委託組織BのERP DB]         [自治体の気象DB]
┌─────────────────┐      ┌─────────────────┐      ┌─────────────────┐
│ 個人の作業記録   │      │ 受注・納品管理   │      │ 雨量・気温データ │
│ 個人のスキル情報 │      │ 人員スケジュール │      │ 防災情報         │
│ 個人の給与情報   │      │ 財務データ       │      │ 観測履歴         │
└─────────────────┘      └─────────────────┘      └─────────────────┘
     私有DB                     私有DB                    公有DB
```

一つのデータベースに全てを集約しようとすると、以下の問題が生じる。

- **法的問題**: 個人情報保護法（GDPR、APPI等）に違反する恐れがある
- **技術的問題**: 異なる組織のデータベースを一元管理する権限を持たない
- **運用問題**: 一つのデータベースの障害が全アクターに影響する（単一障害点）
- **パフォーマンス問題**: 全アクターの状態を一つのDBで管理するのは非現実的

**理由3: 業務実態空間の自律性**

各アクターは一定の自律性を持って動作しており、外部からの干渉を常に許容するわけではない。

```
シーケンサー機械:
  - 内部コントローラで自立動作
  - 外部からの指令は限られたインターフェース経由のみ

AI機械:
  - 内部モデルで自律的判断
  - API経由でのみ外部と通信

従事者（人）:
  - 人間の意思決定を含む
  - 完全に外部から制御可能ではない
```

#### 1.8.4 状態空間データネットへの参加と非参加

上記の制約を踏まえると、業務実態空間の状態管理は**各アクターが各自のデータベースで行う**のが唯一の現実解である。

重要なのは、**各アクターが孤立しているか、状態空間データネットと信頼関係を結んでいるか**という点である。

```
┌─────────────────────────────────────────────────────────────────────┐
│                        状態空間データネット                          │
│                                                                     │
│  ┌───────────┐  ┌───────────┐  ┌───────────┐  ┌───────────┐       │
│  │ トークンA  │  │ トークンB  │  │ トークンC  │  │ トークンD  │  ...    │
│  └───────────┘  └───────────┘  └───────────┘  └───────────┘       │
│                                                                     │
│  ↓ 参照・同期 ↓                                                     │
└───────┬─────────────────────────────────────────────────────────────┘
        │
        │ 信頼関係（参加）
        │
   ┌────┴────────────────────────────────────────────────────┐
   │                                                         │
   ▼                                                         ▼
┌─────────────────┐                              ┌─────────────────┐
│  状態空間ネット  │                              │  孤立DB          │
│  参加アクター    │                              │  非参加アクター   │
│                 │                              │                 │
│ ┌─────────────┐ │                              │ ┌─────────────┐ │
│ │ 業務アプリ   │ │ ←── 信頼関係 ──────────────→ │ │ 旧システム   │ │
│ │ （ERP）      │ │                                │ │ （レガシー） │ │
│ │             │ │                                │ │             │ │
│ │ ・トークン   │ │                                │ │ ・トークン   │ │
│ │   参照可能   │ │                                │ │   参照不可   │ │
│ │ ・同期可能   │ │                                │ │ ・同期不可   │ │
│ └─────────────┘ │                                │ └─────────────┘ │
│                 │                                │                 │
│ ┌─────────────┐ │                                │ ┌─────────────┐ │
│ │ IoT平台     │ │ ←── 信頼関係 ──────────────→ │ │ 個別センサー │ │
│ │             │ │                                │ │ （スタンドアロン）│
│ │ ・トークン   │ │                                │ │             │ │
│ │   参照可能   │ │                                │ │ ・トークン   │ │
│ │ ・同期可能   │ │                                │ │   参照不可   │ │
│ └─────────────┘ │                                │ ・同期不可   │ │
│                 │                                │ └─────────────┘ │
└─────────────────┘                                └─────────────────┘
```

**状態空間データネット参加アクターの特徴:**

| 特徴 | 説明 |
|------|------|
| **信頼関係** | 状態空間データネットとの間で、データの参照・書き込みに関する信頼関係が定義されている |
| **トークン参照可能** | 状態空間データネット上のトークンを参照できる。ナビゲーターが判断するための情報源となる |
| **同期可能** | 物理界の変化をトークンとして状態空間データネットに反映できる。フィードバックループが機能する |
| **API/I/F経由接続** | REST API、GraphQL、gRPC、MQTT等、標準化されたインターフェースで接続されている |
| **状態の部分的可視性** | 全状態が公開されるわけではなく、ナビゲーションに必要な最小限の状態だけがトークンとして公開される |

**孤立DB（非参加アクター）の特徴:**

| 特徴 | 説明 |
|------|------|
| **信頼関係なし** | 状態空間データネットとの間に信頼関係が定義されていない |
| **トークン参照不可** | 状態空間データネット上のトークンを直接参照できない |
| **同期不可** | 物理界の変化を自動的にトークンとして反映できない |
| **独自I/F** | 独自のインターフェースを持っており、標準化されていない |
| **状態の不透明性** | 内部状態が外部から見えない。ナビゲーションの情報源として使えない |

#### 1.8.5 参加・非参加の移行戦略

孤立DBを状態空間データネットに参加させることは、必ずしも容易ではない。段階的な移行が必要である。

```
段階1: 手動エクスポート
  孤立DB → 管理者が手動でデータをエクスポート → トークンに変換 → 状態空間データネットに投入

段階2: バッチ同期
  孤立DB → 定期バッチでデータ抽出 → トークンに変換 → 状態空間データネットに反映

段階3: API接続
  孤立DB → APIゲートウェイ経由で接続 → リアルタイムでトークンを参照・更新

段階4: フル参加
  孤立DB → 状態空間データネットの正式メンバーとして参加
  トークンの双方向同期、イベントベースの通知、信頼関係の確立
```

段階1〜2の間は、**「部分的参加」**と呼べる状態である。リアルタイム性は低いものの、トークンとしての可視性は確保される。

段階3〜4に至っては、**「フル参加」**と呼べる状態であり、フィードバックループが完全に機能する。

#### 1.8.6 物理界の状態変化とナビゲーション

ナビゲーションによって実働者が操作された場合、物理界の状態が変化する。

```
ナビゲーション → 実働者の操作 → 物理界の状態変化

例: 散布ナビゲーション

① ナビゲーターが判断 → 「農薬Aを10aに50ml散布」
② 従事者（人）が手順書を読む
③ 従事者が農薬を調合し、散布機で散布
④ 物理界の状態が変化する:
   - 病害虫数が減少
   - 農薬残留量が変化
   - 従事者の作業時間が経過
⑤ 変化が検知される:
   - センサー（状態空間ネット参加）→ トークン更新（リアルタイム）
   - 従事者の日報入力（孤立DB）→ 手動トークン更新（遅延あり）
   - 気象観測データ（公有DB、状態空間ネット参加）→ トークン更新（リアルタイム）
⑥ 新しいトークンが状態空間データネットに反映
⑦ 新しい状態でナビゲーターが発火
```

**重要な点**: 状態変化の検知と反映の速度は、アクターが状態空間データネットに参加しているかどうかで大きく異なる。

| アクター種別 | 状態空間ネット参加 | 変化検知の速度 | トークン反映の速度 |
|-------------|------------------|--------------|-----------------|
| センサー | 参加 | リアルタイム | リアルタイム |
| 業務アプリケーション | 参加 | リアルタイム〜秒単位 | リアルタイム〜秒単位 |
| AI機械 | 参加 | リアルタイム | リアルタイム |
| シーケンサー機械 | 参加/非参加 | 秒単位〜分単位 | 秒単位〜分単位 |
| 従事者（委託組織） | 非参加/部分的参加 | 日単位 | バッチ単位 |
| 従事者（人） | 非参加 | 日単位〜週単位 | 手動 |
| 実働環境信号 | 参加 | リアルタイム | リアルタイム |

#### 1.8.7 信頼関係の定義

状態空間データネットとアクターの間の信頼関係は、以下のように定義される。

```
信頼関係の定義:

TrustRelationship {
    actor_id: string              // アクターの識別子
    actor_type: string            // アクター種別
    participation_level: enum     // 参加レベル: "none" | "partial" | "full"

    // 許可される操作
    permissions: {
        read_tokens: boolean      // トークンの参照を許可するか
        write_tokens: boolean     // トークンの書き込みを許可するか
        subscribe_events: boolean // イベント購読を許可するか
        receive_navigation: boolean // ナビゲーションの受信を許可するか
    }

    // 公開されるトークンのスコープ
    token_scope: TokenScope[]     // このアクターが公開するトークンの種類

    // 接続インターフェース
    interface: {
        type: enum                // "rest_api" | "graphql" | "grpc" | "mqtt" | "batch"
        endpoint: string          // エンドポイントURI
        auth_method: enum         // "oauth2" | "api_key" | "mutual_tls" | "none"
    }

    // 同期ポリシー
    sync_policy: {
        mode: enum                // "realtime" | "periodic" | "event_driven" | "manual"
        interval_seconds: int     // 同期間隔（modeがperiodicの場合）
        retry_count: int          // リトライ回数
        timeout_seconds: int      // タイムアウト時間
    }
}
```

この信頼関係が定義されていないアクターは、状態空間データネットにとって**「見えない存在」**となる。ナビゲーションの判断材料として使えず、フィードバックループにも参加できない。

---

### 1.9 三層アーキテクチャの再考：分散状態管理を考慮した版

前述の分散状態管理の洞察を踏まえ、三層アーキテクチャを再構成する。

```
┌──────────────────────────────────────────────────────────────────────────┐
│                    状態空間データネット（記号界）                           │
│                                                                          │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │ 参加アクターからのトークン収集・統合・時系列記録                    │   │
│  │ 孤立DBからのトークンは、手動/バッチ/API経由で部分的に収集           │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│                                                                          │
│  ↓ トークン供給                                                          │
└───────────────────────┬──────────────────────────────────────────────────┘
                        ↓ トークン入力
┌───────────────────────┴──────────────────────────────────────────────────┐
│                    ナビゲーターネット（記号界）                             │
│                                                                          │
│  ・純粋関数ネット                                                         │
│  ・状態空間データネットのトークンのみを判断材料とする                       │
│  ・孤立DBの状態は、直接参照できない（信頼関係がないため）                    │
│                                                                          │
│  認知 → 評価 → 決定（仕様決定）→ 投射（テンプレートインスタンス化）       │
│                                                                          │
└───────────────────────┬──────────────────────────────────────────────────┘
                        ↓ 指示
┌───────────────────────┴──────────────────────────────────────────────────┐
│                    物理界（実働）                                           │
│                                                                          │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │ 状態空間ネット参加アクター（リアルタイムフィードバック）            │   │
│  │                                                                  │   │
│  │  • センサー（温湿度、土壌水分、画像）                              │   │
│  │  • 業務アプリケーション（ERP、在庫管理、メール配信）               │   │
│  │  • AI機械（自律ドローン、AI判別カメラ）                            │   │
│  │  • シーケンサー機械（PLC、産業用ロボット）※一部参加                │   │
│  │                                                                  │   │
│  │  ↓ リアルタイムまたは高頻度のトークン反映                          │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│                                                                          │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │ 孤立DBアクター（遅延フィードバック）                               │   │
│  │                                                                  │   │
│  │  • 従事者（人）※日報・手動入力                                     │   │
│  │  • 従事者（委託組織）※契約ベースの報告                             │   │
│  │  • 旧システム（レガシー）※バッチエクスポート                       │   │
│  │                                                                  │   │
│  │  ↓ 日単位〜週単位のトークン反映（手動またはバッチ）                  │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│                                                                          │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │ 実働環境信号（公的データソース）                                    │   │
│  │                                                                  │   │
│  │  • 気象情報（気象庁）                                              │   │
│  │  • 地震計、洪水水位計                                              │   │
│  │  • 放射線計                                                        │   │
│  │                                                                  │   │
│  │  ↓ リアルタイム（公的DBとの信頼関係あり）                           │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│                                                                          │
└──────────────────────────────────────────────────────────────────────────┘
```

**分散状態管理を考慮したアーキテクチャの重要な原則:**

1. **状態空間データネットは、参加アクターからのトークンのみを直接参照する**
   - 孤立DBの状態は、直接参照できない
   - 孤立DBからの情報は、手動/バッチ/API経由で部分的に収集される

2. **ナビゲーターは、状態空間データネットのトークンのみを判断材料とする**
   - ナビゲーター自身はステートレスであり、直接物理界を見ない
   - 物理界の状態は、すべてトークンを通じて間接的にしか参照できない

3. **フィードバックループの速度はアクターによって異なる**
   - 参加アクター: リアルタイム〜秒単位
   - 部分的参加アクター: 分単位〜日単位
   - 非参加アクター: 日単位〜週単位（手動）

4. **信頼関係は動的に管理される**
   - アクターはいつでも参加・離脱できる
   - 信頼関係のレベル（none/partial/full）は動的に変更可能
   - 信頼関係の失効（期限切れ、セキュリティ侵害）も管理される

5. **物理界の全状態を網羅することは目指さない**
   - ナビゲーションに必要な最小限の状態のみを対象とする
   - 不要な状態まで収集・管理しようとするのは非効率

#### 作業者向けテンプレートの具体例

```
【作業手順書】
ステップ1: 保護具を着用（マスク、手袋、ゴーグル）
ステップ2: 農薬タンクにマラソン乳剤を50ml注入
ステップ3: 水で希釈し、総量を10Lとする
ステップ4: 散布機を起動し、歩行速度1m/sで移動
ステップ5: 面積10aをカバーするよう往復散布
注意: 風速3m/s以上の場合は散布を中止
```

#### 作業機械向けテンプレートの具体例

```json
{
  "drone_id": "DRONE-001",
  "flight_plan": {
    "waypoints": [
      {"lat": 35.681236, "lon": 139.767125, "altitude": 5},
      {"lat": 35.681300, "lon": 139.767200, "altitude": 5}
    ],
    "speed_ms": 1.0,
    "flow_rate_ml_min": 100
  },
  "spray_volume": {"total_ml": 50, "area_a": 10}
}
```

#### 作業ソフト向けテンプレートの具体例

```http
POST /api/v1/orders HTTP/1.1
Host: inventory.example.com
Authorization: Bearer <token>
Content-Type: application/json

{
  "product_id": "PROD-001",
  "quantity": 50,
  "priority": "normal",
  "trigger": "auto_reorder"
}
```

---

## 2. 生成AI「エージェント」との違い

### 2.1 見た目が同じでも、中身は全く違う

ナビゲーター自体は行動しない。しかし、**出力先の性質によって、結果が「自律行動」に見える**。

```
ナビゲーター（計算して示すだけ）
    ↓ 作業手順ナビ
    ├─ 作業者 → 人が読んで実行
    │   → 結果：半自律（人が判断挟み込む）
    │
    ├─ 作業機械 → 制御指令が自動実行
    │   → 結果：自律行動のように見える
    │
    └─ 作業ソフト → 操作APIが自動実行
        → 結果：自律行動のように見える
```

### 2.2 生成AIエージェントとRBPナビゲーター＋実働の比較

| 比較項目 | 生成AIエージェント | RBPナビゲーター + 実働 |
|---------|-----------------|---------------------|
| **判断の根拠** | 確率的推論（LLMの重み） | 代数計算（決定論的） |
| **なぜその行動か** | ブラックボックス | 透明（追跡可能） |
| **同じ入力でも** | 毎回違う可能性がある | 必ず同じ出力 |
| **責任所在** | AIが下した判断 | 人が設計した代数ルール |
| **学習能力** | 微調整で変化する | ルール変更のみ（自己学習しない） |
| **テンプレート管理** | プロンプトエンジニアリング | 明示的なテンプレートモジュール |
| **バージョン差異** | モデルの推論に依存 | 境界言語による明示的選択 |

### 2.3 例：在庫管理

**生成AIエージェントの場合：**
```
「在庫が少ない」→ LLMが推論 → 「A商品を発注する」
→ なぜAなのか？ → LLM内部の重みの結果（説明不能）
```

このアプローチの問題点は、判断の根拠が説明不能なことである。なぜA商品を選んだのか、なぜ50個なのか、なぜ今なのか——すべてLLM内部の重みの結果であり、外部からは追跡できない。

**RBPナビゲーターの場合：**
```
「在庫トークン = {A:不足, B:正常, C:危険}」
→ ナビゲーターが計算 → 「A商品: 発注50個, C商品: 緊急発注200個」
→ なぜか？ → 代数行列の演算結果（追跡可能）
→ 作業ソフト（在庫管理システム）が自動実行
→ 発注完了 → 在庫トークンが更新 → 次のナビゲーター発火
```

RBPナビゲーターのアプローチでは、判断の根拠が代数行列の演算結果として追跡可能である。なぜA商品が50個なのか、なぜC商品が緊急発注なのか——すべて行列の演算過程を追えば説明できる。

見た目は同じ「自動発注」でも、**判断の透明性が根本的に違う**。

### 2.4 例：EDIソフト操作（バージョン差異の重要性）

**生成AIエージェントの場合：**
```
「E2Xで取引データをエクスポートして」→ LLMが推論 → API呼び出し
→ E2XのAPIが変わったら？ → 推論が外れる可能性あり
→ Apexに移行したら？ → 再度プロンプトを調整する必要あり
```

**RBPナビゲーターの場合：**
```
「ソフトID=E2X, バージョン=3.2」
→ 選択ブリッジが「edi_e2x_v3_template_group」を選択
→ テンプレートにパラメータを埋め込み → API呼び出し

「ソフトID=Apex, バージョン=5.1」
→ 選択ブリッジが「edi_apex_v5_template_group」を選択
→ 別のテンプレートにパラメータを埋め込み → API呼び出し
```

RBPナビゲーターでは、バージョン差異が**境界言語で明示的に定義**されており、適切なテンプレートモジュールが自動的に選択される。APIが変わっても、テンプレートモジュールを更新すればよく、判断ロジック自体は影響を受けない。

---

## 3. 実装の検証結果

### 3.1 対応できている部分（○）

| モデル要素 | 実装場所 | 内容 |
|-----------|---------|------|
| 状態{トークン} | `rbp_types.py:64` `EntryVector` | 10次元0/1ベクトル、病害虫の活動状態をマーキング |
| ナビゲーター | `bridges.py:47` `spec_bridges` | 6段のBridge（関数＝lambda）、各々が判断＋実働指示 |
| 決定（仕様決定） | RBP行列演算 | 仕様パラメータを出力 |
| 投射（テンプレートインスタンス化） | 複数テンプレートモジュール群 | 仕様決定のパラメータを受け取り、対象ごとのテンプレートに埋め込み手順を生成。バージョン差異（例: EDIソフト E2X vs Apex）ごとに別モジュールを事前準備。選択はブリッジ経由 |

### 3.2 ギャップとして浮上した部分（△）

#### ギャップ1: 認知のNGが「ログ＋終了」になっていない

**現状:**
`main.py:116-129` の `match_eval_box` が認知チェックを担当。UNDEFINED（未知の病害虫組み合わせ）でもログも出さず、`build_prescription` が走り続ける。`api.py:94-95` で並列実行、結果はJSONに載るだけ。

**問題点:**
- UNDEFINEDの入力が来ても、誰もそれを検知していない
- JSONレスポンスの中にUNDEFINEDの結果が混ざっているだけで、それが「異常」であることが明示されていない
- 運用者が気づかないまま、無効な prescribing が行われるリスクがある

**あるべき姿:**
- 認知NGの場合は、即座にログを出力し、処理を終了する
- ログには「UNDEFINED: 原因となったトークン内容」を含める
- APIレスポンスでは、`status: "cogniton_failed"` と明確に区別する

**設計指針:**
```
認知チェック結果:
  ├─ OK → 評価パイプラインへ進捗
  └─ NG → ログ出力 + レスポンス返却 + 処理終了
```

#### ギャップ2: 認知がブリッジパイプラインの外にある

**現状:**
`match_eval_box` はBridgeチェーンとは別パスにある。認知チェック → Bridgeチェーン → 結果取得、という二段構えになっている。

**問題点:**
- 認知チェックがパイプラインの外にあるため、認知NGの場合でもBridgeチェーンが走ってしまう無駄がある
- 認知チェックと評価処理の責務が分離されすぎており、全体のフローが見通しにくい

**あるべき姿:**
認知チェック自体が最初のブリッジ（`COGNITION-BRIDGE`）としてパイプラインに組み込まれる。

```
[COGNITION-BRIDGE] → [EVALUATION-BRIDGE-1] → [EVALUATION-BRIDGE-2] → ...
       ↓ OK                          ↓
       └─────────────────────────────┘
```

こうすれば、認知NGの場合は最初のブリッジで即座に処理が終了し、残りのブリッジは実行されない。

#### ギャップ3: マーキングの「記録・時系列追跡」がない

**現状:**
`EntryVector` は不変（frozen）、入力専用。`BridgeContext` に一部状態はあるが、マーキングの時系列記録ではない。

**問題点:**
- 過去の状態遷移を追跡できない
- どの状態からどの状態に変化したかがわからない
- 異常検知のために状態の推移を見る必要がある場合に、データが存在しない

**あるべき姿:**
状態空間データネットがマーキングの時系列記録を担う。

```
時刻t1: EntryVector(A=1, B=0, C=0, ...)  → 記録
時刻t2: EntryVector(A=1, B=1, C=0, ...)  → 記録
時刻t3: EntryVector(A=0, B=1, C=1, ...)  → 記録
```

この時系列記録があれば、状態の推移を分析したり、異常を検知したりできる。

#### ギャップ4: 実働パートが未実装

**現状:**
現在の `api.py` の出力はJSONレスポンス（判断の結果）であり、**実働の手順書**ではない。

**問題点:**
- ナビゲーターの出力はJSONだが、それは「判断の結果」であって「実働の手順書」ではない
- 作業者向けの手順書（自然言語 + 数値 + 順序）が生成されていない
- 作業機械向けの制御指令（座標 + 流量 + 時間）が生成されていない
- 作業ソフト向けの操作API（パラメータ + 実行トリガー）が生成されていない

**あるべき姿:**
ナビゲーターの出力は、実働カテゴリに応じた形式で生成される。

```
ナビゲーター出力:
  ├─ 作業者向け: 作業手順書（テキストファイルまたは画面表示）
  ├─ 作業機械向け: 制御指令（JSONまたはProtobuf）
  └─ 作業ソフト向け: API呼び出し（HTTPリクエスト）
```

#### ギャップ5: 物理界の変化を記号界に反映する機構がない

**現状:**
物理界の変化を記号界の状態空間に反映する機構がない。

**問題点:**
- 作業者が農薬を散布して病害虫が減ったとしても、その情報がシステムに反映されない
- 作業機械が灌漑を開始したとしても、その状態がトークンに反映されない
- 作業ソフトが発注APIを叩いたとしても、発注完了の情報が次の判断に使われない

**あるべき姿:**
物理界の変化が検知され、記号化されて状態空間データネットに反映される。

```
物理界の変化検知 → 記号化（センサー値→トークン） → 状態空間データネット更新
```

具体的な変換例:
- 作業者の実働完了 → 完了レポート → トークン更新（例: `散布完了=True`）
- 作業機械のセンサー値 → 生データ → トークン合成（例: 湿度センサー値 → 湿潤レベル）
- 作業ソフトのAPI応答 → JSONパース → トークン合成（例: 発注完了 → `発注ステータス=完了`）

#### ギャップ6: 状態空間データネットが未実装

**現状:**
ナビゲーターネットを純粋関数にするための状態管理層がない。

**問題点:**
- ナビゲーターが内部状態を持つべきでない（ステートレスであるべき）が、現状では状態管理が適切に分離されていない
- マーキングの記録・時系列追跡を状態空間データネットが担当すべきだが、そのような層が存在しない
- ナビゲーターネットは状態空間データネットからトークンを読み取るべきだが、直接入力を受けている

**あるべき姿:**
状態空間データネットが独立した層として実装される。

```
状態空間データネット:
  ├─ トークン状態の記録・保持
  ├─ マーキングの時系列記録
  ├─ ナビゲーターへのトークン供給
  └─ 物理界からのフィードバック受信・トークン合成

ナビゲーターネット:
  ├─ 状態空間データネットからトークンを読み取る
  ├─ 純粋関数として計算
  └─ 結果（作業手順ナビ）を返す
```

**追加の考察: 分散状態管理と参加/非参加の概念**

業務実態空間の全状態を一つのデータベースで管理することは不可能である。各アクターは各自のデータベースを持ち、**状態空間データネットと信頼関係を結んでいるか（参加）、孤立しているか（非参加）**という二つの状態に分かれる。

- **参加アクター**: トークンの参照・書き込みが可能。フィードバックループが機能する。
- **非参加アクター**: トークンの参照・書き込みができない。フィードバックループに参加できない（手動/バッチ経由の部分的参加はあり得る）。

状態空間データネットの設計では、この参加/非参加の概念を明確にし、信頼関係の定義・管理・移行戦略を設計する必要がある。

#### ギャップ7: 投射テンプレートモジュール群とその選択ブリッジが未設計

**現状:**
投射は単一モジュールではなく、**対象ごと・バージョンごとに個別に準備するテンプレートモジュール群**である。しかし、現在、テンプレートモジュールの設計思想すら文書化されていない。

**問題点:**
- 同じEDIソフトでもE2XとApexでAPIが異なる → 別々のテンプレートモジュールが必要だが、その設計がない
- メール送信エージェントには本文・宛先・件名の各テンプレートが必要だが、その設計がない
- **どのテンプレートモジュールを使うか**の選択は、仕様決定→投射間のブリッジが行うべきだが、そのブリッジの設計がない
- この選択基準は**境界言語（DSL）**で明示的に定義する必要があるが、境界言語の設計がない

**あるべき姿:**

1. **テンプレートモジュールの設計**: 対象ごと・バージョンごとにテンプレートモジュールを定義する
2. **選択ブリッジの設計**: 仕様決定の結果を見て、どのテンプレートモジュールを使うかを判断するブリッジを設計する
3. **境界言語の設計**: テンプレート選択基準を境界言語で定義する

**詳細設計の方向性:**

```
テンプレートモジュールの構造:

TemplateModule {
    module_id: string          // 一意の識別子（例: "edi_e2x_v3_export"）
    target_software: string    // ターゲットソフト（例: "E2X"）
    target_version: string     // ターゲットバージョン（例: "3.x"）
    operation_type: string     // 操作タイプ（例: "export"）
    template_content: Template // テンプレート本体
    parameters: ParameterSchema // 必要なパラメータのスキーマ
}

選択ブリッジのロジック:

SelectTemplateBridge(spec_params) {
    // 仕様決定の結果からターゲット情報を抽出
    target = spec_params.target_software
    version = spec_params.target_version
    operation = spec_params.operation_type

    // 境界言語のルールに基づいてテンプレートモジュールを選択
    template_module = lookup_template(target, version, operation)

    // テンプレートにパラメータを埋め込み
    navigation = instantiate(template_module, spec_params.parameters)

    return navigation
}
```

---

## 4. 全体アーキテクチャの確立

```
┌──────────────────────────────────────────────────────────────────┐
│                    状態空間データネット（記号界）                   │
│                                                                  │
│  状態{トークン}の記録・保持                                       │
│  ナビゲーターネットと常に連動                                     │
│                                                                  │
└───────────────────────┬──────────────────────────────────────────┘
                        ↓ トークン入力
┌───────────────────────┴──────────────────────────────────────────┐
│                    ナビゲーターネット（記号界）                     │
│                                                                  │
│  状態{トークン}充足                                               │
│    → ナビゲーター（判断パート）                                    │
│      → 認知（バリデーションチェック）                              │
│        ├─ OK → 要求評価(RBP)                                      │
│        │       → 仕様決定(RBP)                                     │
│        │         → パイプ/ブリッジ構造（ビジネスロジック）          │
│        │           → 投射（テンプレートに仕様パラメータを埋め込み、手順＝ナビゲーション生成） │
│        │             ↓ 仕様決定→投射間のブリッジで対象・バージョンを選択                  │
│        │    ┌─────────────────────────────────────────────────────┐                    │
│        │    │  投射テンプレートモジュール群（対象ごとに個別準備）    │                    │
│        │    │                                                     │                    │
│        │    │  メール送信テンプレート群                            │                    │
│        │    │    ├─ 本文テンプレート  ├─ 宛先テンプレート          │                    │
│        │    │    └─ 件名テンプレート                              │                    │
│        │    │                                                     │                    │
│        │    │  EDI-E2X操作テンプレート群                           │                    │
│        │    │    ├─ 取引データエクスポート  ├─ 注文書インポート     │                    │
│        │    │    └─ 納品書連携                                          │                    │
│        │    │                                                     │                    │
│        │    │  EDI-Apex操作テンプレート群（E2Xとは別API）           │                    │
│        │    │    ├─ 取引データエクスポート  ├─ 注文書インポート     │                    │
│        │    │    └─ 納品書連携                                          │                    │
│        │    │                                                     │                    │
│        │    │  散布ドローン制御テンプレート群                       │                    │
│        │    │    ├─ 航路計画  ├─ 流量制御  └─ 着陸判定             │                    │
│        │    │                                                     │                    │
│        │    │  ...（対象・バージョンごとに個別に準備）               │                    │
│        │    └─────────────────────────────────────────────────────┘                    │
│        │             ↓                                             │
│        │    ┌────┬────┬────┐                                      │
│        │    ▼    ▼    ▼    ▼                                      │
│        │  作業者 作業機械 作業ソフト                               │
│        └─ NG → ログ → 終了                                        │
│              ↓                                                    │
│  フィードバック（物理界の変化を記号化）                             │
│    ↓                                                            │
│  新しい状態{トークン} → 状態空間データネットに反映                │
│                                                                  │
│  ↓ 指示 ↓                                                       │
└───────────────────────┬──────────────────────────────────────────┘
                        ↓ 指示
┌───────────────────────┴──────────────────────────────────────────┐
│                    物理界（実働）                                  │
│                                                                  │
│  作業者が農薬を散布する                                           │
│  作業機械が灌漑を開始する                                          │
│  作業ソフトが発注APIを叩く                                         │
│                                                                  │
│  ↓ 物理界の状態が変化する                                          │
│  （病害虫数の減少、在庫の増加、配送の完了）                         │
│                                                                  │
│  ↓ 変化が検知され、記号化される                                    │
│  （センサー値、DB更新、API応答）                                    │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
                        ↑
                フィードバックループ
          （物理界の変化 → 記号界の状態空間更新）
```

### 4.1 各コンポーネントの詳細

- **状態空間データネット**: トークン状態の記録・保持。ナビゲーターネットと常に連動。ナビゲーターは状態を持たない（ステートレス）。マーキングの時系列記録も担当する。**業務実態空間のアクターが状態空間データネットに参加しているか（信頼関係を結び、トークンの参照・書き込みが可能）、孤立しているか（非参加）を管理する**。参加アクターからのフィードバックはリアルタイム〜秒単位で反映され、非参加アクターからの情報は手動/バッチ経由で部分的に収集される。

- **ナビゲーターネット**: 純粋関数ネット。モナドから解放された刺激・反射ネットワーク。内部状態を持たず、入力→計算→出力のみ。

- **認知**: トークン（作業要求状態）のバリデーションチェック。OKなら要求評価へ、NGならログ出力後終了。本来はブリッジパイプラインの最初のブリッジ（COGNITION-BRIDGE）として実装されるべき。

- **要求評価**: 代数設計された行列演算（要求評価RBP行列）。複雑な要求を評価BOXに集約する効果。

- **決定（仕様決定）**: 代数設計されたRBP行列演算。要求評価の結果を入力として受け取り、作動の仕様パラメータを決定。

- **投射（テンプレートインスタンス化）**: 仕様決定で出力されたパラメータを受け取り、具体的な手順＝ナビゲーションを生成するモジュール。**単一のモジュールではなく、対象ごとに個別に準備する複数のテンプレートモジュールからなる**。例えば、メール送信エージェントには「メール本文テンプレート」「宛先指定テンプレート」「件名生成テンプレート」を保持し、それぞれにパラメータを埋め込んで実行可能な指示を生成する。ソフトウェア操作エージェントの場合、同じEDIソフトでもバージョンが異なればAPIが異なる（E2XとApexなど）。そのため、**各バージョンごとに別のテンプレートモジュールを事前に準備する**。どのテンプレートモジュールを使うかの選択は、**仕様決定フェーズと投射フェーズの間のブリッジ**によって行われ、この選択基準は**境界言語（DSL）**で明示的に定義される。

- **パイプ/ブリッジ構造**: 仕様決定以降、RBPのパイプとブリッジ構造でビジネスロジックを代数的に実装。

- **境界言語（DSL）**: 要求評価も仕様決定も行列表現されるが、人には意味がわからない。境界言語で記述し、**ミラーID**を持つ。ミラーIDが要求評価→仕様決定への入力となる。また、テンプレート選択ブリッジの選択基準も境界言語で定義される。

- **実働**: ナビゲーターの指示を受けて、作業者 / 作業機械 / 作業ソフトが業務実態空間で実行。

- **フィードバック**: 実働完了後の物理界の変化が、新しいトークンとして状態空間データネットに反映される。

---

## 5. 次回への課題

### 優先度高（すぐに着手すべき課題）

- **認知チェックをブリッジパイプラインに統合する設計**
  - COGNITION-BRIDGEとして実装する
  - NGの場合のログ出力・処理終了フローを設計する
  - APIレスポンスでの明確な区別（`cogniton_failed`）を定義する

- **境界言語（DSL）とミラーIDの詳細設計**
  - ミラーIDの命名規則とスコープを定義する
  - 要求評価→仕様決定への入力としてのミラーIDの使い方を設計する
  - テンプレート選択ブリッジの選択基準を境界言語で定義する方法を設計する

- **投射テンプレートモジュール群の設計**
  - 対象ごと・バージョンごとにテンプレートモジュールを定義する
  - テンプレートモジュールの構造（module_id, target_software, target_version, operation_type, template_content, parameters）を設計する
  - テンプレートインスタンス化のプロセスを設計する

### 優先度中（基礎設計が整ってから着手）

- **要求評価RBP行列と仕様決定RBP行列の実装位置の特定**
  - どのBridgeが要求評価を担当し、どのBridgeが仕様決定を担当するかを特定する
  - 行列の設計と実装位置を決定する

- **仕様決定→投射間の選択ブリッジの設計**
  - 仕様決定の結果を見て、どのテンプレートモジュールを使うかを判断するブリッジを設計する
  - 選択ロジックの実装方法を決定する

- **状態空間データネットの設計**
  - マーキング記録・時系列追跡・ナビゲーターとの連動を設計する
  - データストアの選定（SQLite, PostgreSQL等）を検討する
  - トークンの永続化・復元方法を設計する
  - **分散状態管理の設計**: 各アクターが各自のDBを持つ前提の設計
  - **参加/非参加の概念設計**: 信頼関係の定義・管理・移行戦略
  - **信頼関係スキーマの設計**: TrustRelationshipの構造定義
  - **参加レベルの定義**: none / partial / full の3段階と各段階の要件
  - **フィードバック速度の差異設計**: 参加アクター（リアルタイム）vs 非参加アクター（日単位〜週単位）

- **実働パートの設計**
  - 作業者向け手順書の生成フォーマットを設計する
  - 作業機械向け制御指令のフォーマットを設計する
  - 作業ソフト向け操作APIの呼び出し方法を設計する

### 優先度低（基盤が整ってから着手）

- **フィードバックループの設計**
  - 物理界→記号界の変換方法を設計する
  - 変化検知のトリガーを設計する（ポーリング、Webhook、MQTT等）
  - トークン合成の方法を設計する

- **トークン生成のトリガー**
  - 誰が・いつ・どのように新しい状態を生成するかを設計する
  - 定期監視によるトークン生成
  - イベント駆動によるトークン生成
  - 手動によるトークン生成

---

## 6. 並列性（分業）問題 — 解決済み

### 6.1 問題（当時）

業務実態空間は、複数の人・機械・組織などが**分業**をしている空間である。分業は本質的に並列（複数のアクターが同時に別々の作業を進める）だが、現在のナビゲーションネットの実装はペトリネットモデルをベースにしていながら並列を扱えない。

原因を実装で確認したところ、これはペトリネット理論そのものの限界ではなく、`framework/rbp_core.js`（39〜51行目）が採用している実装上の制約によるものだった：

- BRIDGEはlevel昇順にソートされた**単一の直列チェーン**として実行される（`sorted = [...bridges].sort((a, b) => a.level - b.level)`）
- `direction`は`'forward'`固定（逆止弁制約）
- `level`はstrictly increasing必須（循環なし制約）

つまり現行実装は「分岐もマージもない、1本道のBRIDGE通過」のみをサポートする、ペトリネットの中でも特殊な部分集合しか実装していない。本来のペトリネットはplace/transitionの汎用グラフであり、以下の並列パターンを自然に表現できる：

- **AND-split（分岐/分業の開始）**: 1つのtransitionが複数のplaceへ同時にトークンを出力する（例：1つの作業要求から作業者Aと作業者Bへ並行して作業指示が分かれる）
- **AND-join（合流/分業の同期）**: 1つのtransitionが複数のplace全てにトークンが揃うまで発火しない（例：作業者Aと作業者Bの両方の作業完了を待って次のBRIDGEに進む）

### 6.2 解決策

**投射の定義を変更するだけで並列性が実現する。**

```
旧:  投射 → 1つの手順のみ
新:  投射 → 2つ以上の手順を所有可能
```

**並列の仕組み:**

```
ブリッジ（transition）
  ↓ 1つの仕様を決定
投射（place）← ここが変更点！
  ├─ 手順A  ← 並列実行
  ├─ 手順B  ← 並列実行
  └─ 手順C  ← 並列実行
```

- **ブリッジ**は相変わらず1つの仕様しか決定しない
- その仕様が「手順を複数持つ投射」を選べば、その投射内の手順が**すべて並列に実行される**
- 合流（AND-join）はPetri netの定義通り：次のtransitionが入力placeのトークンが揃うまで待つ。追加の仕組みは不要

**分岐はブリッジで、並列は投射で:**

```
分岐（パッケージ切り替えなど）:
  仕様決定 → [選択ブリッジA] → 投射A（E2X用）
            [選択ブリッジB] → 投射B（Apex用）
            ← これはブリッジ層で分岐

並列（分業）:
  仕様決定 → [選択ブリッジ] → 投射（作業者Aの手順 + 作業者Bの手順）
            ← 1つのブリッジが1つの投射を選び、その投射内で並列
```

### 6.3 設計判断

| 判断 | 内容 |
|------|------|
| **変更範囲** | 投射のデータ構造（単一→配列）と実行エンジン（逐次→並列）のみ |
| **ブリッジ層** | 変更なし。既存の制約（level strictly increasing, direction forward）を維持 |
| **AND-join** | Petri netの定義通り。追加機構不要 |
| **分岐 vs 並列** | 責任分離が明確。分岐＝ブリッジ層、並列＝投射層 |

### 6.4 実装上の変更範囲

1. **投射のデータ構造** — 新規定義。手順の配列を持つ
2. **実行エンジン** — 投射実行時に手順を並列実行（JSならPromise.all、Pythonならasyncio.gather）
3. **AND-joinの待ち合わせ** — 次のブリッジが「全入力placeのトークン待ち」を実装。既存の線形チェーン実行ロジックには手を入れない

### 6.5 今後の検討事項

- **「手順」の型定義**がまだない。手順が何を返すか（成功/失敗/ステータス）、エラー時の振る舞い（一部失敗で全体をabortするか、部分的結果を返すか）を設計する必要がある
- **AND-joinの表現**が「Petri netの定義通り」と言いますが、現行の線形チェーンにはplace/transitionの概念がありません。並列実行後の同期をどう実装するかは別途考える必要があります（投射の全手順完了を待つ、という単純な実装で十分なら問題ない）
